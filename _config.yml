# === the basics ===
title: kamila.is
permalinks: pretty

# === defaults ===
defaults:
  - scope: {path: ""}
    values:
      layout: default

# === Build settings ===
exclude: [Gemfile, Gemfile.lock]
