# === the basics ===
title: kamila.is
email: kamila@ksp.sk
permalink: pretty
repository: anotherkamila/kamila.is

# === defaults ===
defaults:
  - scope: {path: ""}
    values:
      layout: default

# === Build settings ===
excerpt_separator: <!--more-->
exclude: [Gemfile, Gemfile.lock]
plugins:
  - jekyll-sitemap
