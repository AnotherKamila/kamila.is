# === the basics ===
title: kamila.is
url: ""
permalinks: pretty
repository: anotherkamila/kamila.is

# === defaults ===
defaults:
  - scope: {path: ""}
    values:
      layout: default

# === Build settings ===
markdown: kramdown
kramdown:
  smartypants: true
plugins: [bootstrap, autoprefixer-rails, jekyll-assets]
sass:
    sass_dir: _sass
exclude: [Gemfile, Gemfile.lock]
